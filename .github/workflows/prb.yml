# Do not modify!
# This file was generated from a template using https://github.com/StefMa/pkl-gha

name: Pull Request
'on':
  pull_request: {}
permissions:
  contents: read
jobs:
  gradle-check:
    runs-on: ubuntu-24.04
    env:
      LANG: en_US.UTF-8
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: x64
        cache: gradle
    - name: check
      shell: bash
      run: ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true check
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-gradle-check
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-gradle-check
        path: '*/build/test-reports/**/*'
  gradle-check-windows:
    if: startsWith(github.event.comment.body, '/build windows')
    runs-on: windows-latest
    env:
      LANG: en_US.UTF-8
      JAVA_HOME: /jdk
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: x64
        cache: gradle
    - name: check
      shell: bash
      run: ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true check
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-gradle-check-windows
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-gradle-check-windows
        path: '*/build/test-reports/**/*'
  pkl-cli-linux-amd64-snapshot:
    if: startsWith(github.event.comment.body, '/build native') || startsWith(github.event.comment.body, '/build native-pkl-cli') || startsWith(github.event.comment.body, '/build native-pkl-cli-linux') || startsWith(github.event.comment.body, '/build native-pkl-cli-linux-amd64')
    runs-on: ubuntu-24.04
    env:
      LANG: en_US.UTF-8
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: x64
        cache: gradle
    - name: gradle buildNative
      shell: bash
      run: |-
        export PATH=~/staticdeps/bin:$PATH
        ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true pkl-cli:buildNative
    - name: Upload executable artifacts
      uses: actions/upload-artifact@v5
      with:
        name: pkl-cli-linux-amd64
        path: pkl-cli/build/executable/**/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-pkl-cli-linux-amd64-snapshot
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-pkl-cli-linux-amd64-snapshot
        path: '*/build/test-reports/**/*'
  pkl-cli-macOS-aarch64-snapshot:
    if: startsWith(github.event.comment.body, '/build native') || startsWith(github.event.comment.body, '/build native-pkl-cli') || startsWith(github.event.comment.body, '/build native-pkl-cli-macOS') || startsWith(github.event.comment.body, '/build native-pkl-cli-macOS-aarch64')
    runs-on:
    - self-hosted
    - macos
    env:
      LANG: en_US.UTF-8
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: aarch64
        cache: gradle
    - name: gradle buildNative
      shell: bash
      run: |-
        export PATH=~/staticdeps/bin:$PATH
        ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true pkl-cli:buildNative
    - name: Upload executable artifacts
      uses: actions/upload-artifact@v5
      with:
        name: pkl-cli-macOS-aarch64
        path: pkl-cli/build/executable/**/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-pkl-cli-macOS-aarch64-snapshot
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-pkl-cli-macOS-aarch64-snapshot
        path: '*/build/test-reports/**/*'
  pkl-cli-linux-aarch64-snapshot:
    if: startsWith(github.event.comment.body, '/build native') || startsWith(github.event.comment.body, '/build native-pkl-cli') || startsWith(github.event.comment.body, '/build native-pkl-cli-linux') || startsWith(github.event.comment.body, '/build native-pkl-cli-linux-aarch64')
    runs-on: ubuntu-24.04-arm
    env:
      LANG: en_US.UTF-8
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: aarch64
        cache: gradle
    - name: gradle buildNative
      shell: bash
      run: |-
        export PATH=~/staticdeps/bin:$PATH
        ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true pkl-cli:buildNative
    - name: Upload executable artifacts
      uses: actions/upload-artifact@v5
      with:
        name: pkl-cli-linux-aarch64
        path: pkl-cli/build/executable/**/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-pkl-cli-linux-aarch64-snapshot
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-pkl-cli-linux-aarch64-snapshot
        path: '*/build/test-reports/**/*'
  pkl-cli-linux-alpine-amd64-snapshot:
    if: startsWith(github.event.comment.body, '/build native') || startsWith(github.event.comment.body, '/build native-pkl-cli') || startsWith(github.event.comment.body, '/build native-pkl-cli-linux') || startsWith(github.event.comment.body, '/build native-pkl-cli-linux-amd64')
    runs-on: ubuntu-24.04
    env:
      LANG: en_US.UTF-8
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: x64
        cache: gradle
    - name: Install musl and zlib
      run: |-
        mkdir -p ~/staticdeps/

        # install zlib
        if [[ ! -f ~/staticdeps/include/zlib.h ]]; then
          curl -Lf https://github.com/madler/zlib/releases/download/v1.2.13/zlib-1.2.13.tar.gz -o /tmp/zlib.tar.gz

          mkdir -p /tmp/dep_zlib-1.2.13 \
          && cd /tmp/dep_zlib-1.2.13 \
          && cat /tmp/zlib.tar.gz | tar --strip-components=1 -xzC . \
          && echo "zlib-1.2.13: configure..." && ./configure --static --prefix="$HOME"/staticdeps > /dev/null \
          && echo "zlib-1.2.13: make..." && make -s -j4 \
          && echo "zlib-1.2.13: make install..." && make -s install \
          && rm -rf /tmp/dep_zlib-1.2.13
        fi

        # install musl
        if [[ ! -f ~/staticdeps/bin/x86_64-linux-musl-gcc ]]; then
          curl -Lf https://musl.libc.org/releases/musl-1.2.5.tar.gz -o /tmp/musl.tar.gz

          mkdir -p /tmp/dep_musl-1.2.5 \
          && cd /tmp/dep_musl-1.2.5 \
          && cat /tmp/musl.tar.gz | tar --strip-components=1 -xzC . \
          && echo "musl-1.2.5: configure..." && ./configure --disable-shared --prefix="$HOME"/staticdeps > /dev/null \
          && echo "musl-1.2.5: make..." && make -s -j4 \
          && echo "musl-1.2.5: make install..." && make -s install \
          && rm -rf /tmp/dep_musl-1.2.5

          # native-image expects to find an executable at this path.
          ln -s ~/staticdeps/bin/musl-gcc ~/staticdeps/bin/x86_64-linux-musl-gcc
        fi
    - name: gradle buildNative
      shell: bash
      run: |-
        export PATH=~/staticdeps/bin:$PATH
        ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true -Dpkl.musl=true pkl-cli:buildNative
    - name: Upload executable artifacts
      uses: actions/upload-artifact@v5
      with:
        name: pkl-cli-alpine-linux-amd64
        path: pkl-cli/build/executable/**/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-pkl-cli-linux-alpine-amd64-snapshot
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-pkl-cli-linux-alpine-amd64-snapshot
        path: '*/build/test-reports/**/*'
  pkl-cli-windows-amd64-snapshot:
    if: startsWith(github.event.comment.body, '/build native') || startsWith(github.event.comment.body, '/build native-pkl-cli') || startsWith(github.event.comment.body, '/build native-pkl-cli-windows') || startsWith(github.event.comment.body, '/build native-pkl-cli-windows-amd64')
    runs-on: windows-latest
    env:
      LANG: en_US.UTF-8
      JAVA_HOME: /jdk
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: x64
        cache: gradle
    - name: gradle buildNative
      shell: bash
      run: |-
        export PATH=~/staticdeps/bin:$PATH
        ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true pkl-cli:buildNative
    - name: Upload executable artifacts
      uses: actions/upload-artifact@v5
      with:
        name: pkl-cli-windows-amd64
        path: pkl-cli/build/executable/**/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-pkl-cli-windows-amd64-snapshot
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-pkl-cli-windows-amd64-snapshot
        path: '*/build/test-reports/**/*'
  pkl-doc-linux-amd64-snapshot:
    if: startsWith(github.event.comment.body, '/build native') || startsWith(github.event.comment.body, '/build native-pkl-doc') || startsWith(github.event.comment.body, '/build native-pkl-doc-linux') || startsWith(github.event.comment.body, '/build native-pkl-doc-linux-amd64')
    runs-on: ubuntu-24.04
    env:
      LANG: en_US.UTF-8
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: x64
        cache: gradle
    - name: gradle buildNative
      shell: bash
      run: |-
        export PATH=~/staticdeps/bin:$PATH
        ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true pkl-doc:buildNative
    - name: Upload executable artifacts
      uses: actions/upload-artifact@v5
      with:
        name: pkl-doc-linux-amd64
        path: pkl-doc/build/executable/**/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-pkl-doc-linux-amd64-snapshot
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-pkl-doc-linux-amd64-snapshot
        path: '*/build/test-reports/**/*'
  pkl-doc-macOS-aarch64-snapshot:
    if: startsWith(github.event.comment.body, '/build native') || startsWith(github.event.comment.body, '/build native-pkl-doc') || startsWith(github.event.comment.body, '/build native-pkl-doc-macOS') || startsWith(github.event.comment.body, '/build native-pkl-doc-macOS-aarch64')
    runs-on:
    - self-hosted
    - macos
    env:
      LANG: en_US.UTF-8
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: aarch64
        cache: gradle
    - name: gradle buildNative
      shell: bash
      run: |-
        export PATH=~/staticdeps/bin:$PATH
        ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true pkl-doc:buildNative
    - name: Upload executable artifacts
      uses: actions/upload-artifact@v5
      with:
        name: pkl-doc-macOS-aarch64
        path: pkl-doc/build/executable/**/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-pkl-doc-macOS-aarch64-snapshot
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-pkl-doc-macOS-aarch64-snapshot
        path: '*/build/test-reports/**/*'
  pkl-doc-linux-aarch64-snapshot:
    if: startsWith(github.event.comment.body, '/build native') || startsWith(github.event.comment.body, '/build native-pkl-doc') || startsWith(github.event.comment.body, '/build native-pkl-doc-linux') || startsWith(github.event.comment.body, '/build native-pkl-doc-linux-aarch64')
    runs-on: ubuntu-24.04-arm
    env:
      LANG: en_US.UTF-8
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: aarch64
        cache: gradle
    - name: gradle buildNative
      shell: bash
      run: |-
        export PATH=~/staticdeps/bin:$PATH
        ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true pkl-doc:buildNative
    - name: Upload executable artifacts
      uses: actions/upload-artifact@v5
      with:
        name: pkl-doc-linux-aarch64
        path: pkl-doc/build/executable/**/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-pkl-doc-linux-aarch64-snapshot
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-pkl-doc-linux-aarch64-snapshot
        path: '*/build/test-reports/**/*'
  pkl-doc-linux-alpine-amd64-snapshot:
    if: startsWith(github.event.comment.body, '/build native') || startsWith(github.event.comment.body, '/build native-pkl-doc') || startsWith(github.event.comment.body, '/build native-pkl-doc-linux') || startsWith(github.event.comment.body, '/build native-pkl-doc-linux-amd64')
    runs-on: ubuntu-24.04
    env:
      LANG: en_US.UTF-8
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: x64
        cache: gradle
    - name: Install musl and zlib
      run: |-
        mkdir -p ~/staticdeps/

        # install zlib
        if [[ ! -f ~/staticdeps/include/zlib.h ]]; then
          curl -Lf https://github.com/madler/zlib/releases/download/v1.2.13/zlib-1.2.13.tar.gz -o /tmp/zlib.tar.gz

          mkdir -p /tmp/dep_zlib-1.2.13 \
          && cd /tmp/dep_zlib-1.2.13 \
          && cat /tmp/zlib.tar.gz | tar --strip-components=1 -xzC . \
          && echo "zlib-1.2.13: configure..." && ./configure --static --prefix="$HOME"/staticdeps > /dev/null \
          && echo "zlib-1.2.13: make..." && make -s -j4 \
          && echo "zlib-1.2.13: make install..." && make -s install \
          && rm -rf /tmp/dep_zlib-1.2.13
        fi

        # install musl
        if [[ ! -f ~/staticdeps/bin/x86_64-linux-musl-gcc ]]; then
          curl -Lf https://musl.libc.org/releases/musl-1.2.5.tar.gz -o /tmp/musl.tar.gz

          mkdir -p /tmp/dep_musl-1.2.5 \
          && cd /tmp/dep_musl-1.2.5 \
          && cat /tmp/musl.tar.gz | tar --strip-components=1 -xzC . \
          && echo "musl-1.2.5: configure..." && ./configure --disable-shared --prefix="$HOME"/staticdeps > /dev/null \
          && echo "musl-1.2.5: make..." && make -s -j4 \
          && echo "musl-1.2.5: make install..." && make -s install \
          && rm -rf /tmp/dep_musl-1.2.5

          # native-image expects to find an executable at this path.
          ln -s ~/staticdeps/bin/musl-gcc ~/staticdeps/bin/x86_64-linux-musl-gcc
        fi
    - name: gradle buildNative
      shell: bash
      run: |-
        export PATH=~/staticdeps/bin:$PATH
        ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true -Dpkl.musl=true pkl-doc:buildNative
    - name: Upload executable artifacts
      uses: actions/upload-artifact@v5
      with:
        name: pkl-doc-alpine-linux-amd64
        path: pkl-doc/build/executable/**/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-pkl-doc-linux-alpine-amd64-snapshot
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-pkl-doc-linux-alpine-amd64-snapshot
        path: '*/build/test-reports/**/*'
  pkl-doc-windows-amd64-snapshot:
    if: startsWith(github.event.comment.body, '/build native') || startsWith(github.event.comment.body, '/build native-pkl-doc') || startsWith(github.event.comment.body, '/build native-pkl-doc-windows') || startsWith(github.event.comment.body, '/build native-pkl-doc-windows-amd64')
    runs-on: windows-latest
    env:
      LANG: en_US.UTF-8
      JAVA_HOME: /jdk
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - uses: actions/setup-java@v5
      with:
        java-version: '21.0'
        distribution: temurin
        architecture: x64
        cache: gradle
    - name: gradle buildNative
      shell: bash
      run: |-
        export PATH=~/staticdeps/bin:$PATH
        ./gradlew --info --stacktrace -DtestReportsDir=${HOME}/test-results -DpklMultiJdkTesting=true pkl-doc:buildNative
    - name: Upload executable artifacts
      uses: actions/upload-artifact@v5
      with:
        name: pkl-doc-windows-amd64
        path: pkl-doc/build/executable/**/*
    - name: Upload Test Result XML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-xml-pkl-doc-windows-amd64-snapshot
        path: '*/build/test-results/**/*.xml'
    - name: Upload Test Result HTML
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-html-pkl-doc-windows-amd64-snapshot
        path: '*/build/test-reports/**/*'
  upload-event-file:
    runs-on: ubuntu-latest
    steps:
    - name: Upload event file
      if: '!cancelled()'
      uses: actions/upload-artifact@v5
      with:
        name: test-results-event-file
        path: ${{ github.event_path }}

–– Pkl Error ––
Cannot find property `name` in object of type `pkl.ref#Reference<reference30#D, (Int) -> Boolean>`.

xx | res = myRef.name
           ^^^^^^^^^^
at reference30#res (file:///$snippetsDir/input/errors/reference30.pkl)

Cannot find property `name` in type `(Int) -> Boolean`.

xxx | renderer.renderDocument(value)
      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
at pkl.base#Module.output.text (pkl:base)

xxx | if (renderer is BytesRenderer) renderer.renderDocument(value) else text.encodeToBytes("UTF-8")
                                                                         ^^^^
at pkl.base#Module.output.bytes (pkl:base)

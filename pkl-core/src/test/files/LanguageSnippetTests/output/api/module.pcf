examples {
  ["relativePathTo()"] {
    List("dir1", "dir2")
    List()
    List()
    List()
    "No descendant path exists between modules `file:///$snippetsDir/input/api/dir1/dir2/relativePathTo.pkl` and `file:///$snippetsDir/input/api/module.pkl`."
  }
}

import "pkl:ref"

class A {
  foo: String | Int | Boolean
}

class D extends ref.Domain {
  function renderReference(reference: ref.Reference<this, Any>): String = throw("not supported")
}
local d: D = new {}

test = ref.Reference::<D, A>(d, "").foo as ref.Reference<D, Int | Boolean>

import "pkl:ref"

class D extends ref.Domain {
  function renderReference(_): String = throw("not implemented")
}

local myRef = ref.Reference::<D, Duration | Listing<String>>(new D {}, null)

res = myRef[0]

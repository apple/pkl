import "pkl:ref"

class D extends ref.Domain {
  function renderReference(_): String = throw("not implemented")
}

class Bird {
  name: String
}

myRef: ref.Reference<D, Bird | Int> = ref.Reference::<D, Bird | Int>(new D {}, null)

res = myRef.name
